{
  "name": "@voicepilot/crm-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Canonical CRM model, adapter contract, field mapping and idempotent sync queue",
  "scripts": {
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  }
}
